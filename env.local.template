# Copy to .env.local and fill in your real values (do not commit .env.local)

# Firebase Admin (server-side)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Keep literal \\n for newlines if storing in env files
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\nYOUR_PRIVATE_KEY_CONTENT\\n-----END PRIVATE KEY-----\\n"

# Cashfree Payment Gateway
CASHFREE_APP_ID=
CASHFREE_SECRET_KEY=
# sandbox or production
CASHFREE_ENV=sandbox
# Used by the web app to choose the correct JS SDK
NEXT_PUBLIC_CASHFREE_MODE=sandbox

# Optional: App URLs for payment callbacks
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# APP_BASE_URL=http://localhost:3000

# Optional: Next.js runtime
# NODE_ENV=development
